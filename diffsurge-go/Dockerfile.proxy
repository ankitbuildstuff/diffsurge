# DEPRECATED — replaced by the consolidated Dockerfile.
#
# Build the proxy service with:
#   docker build --build-arg SERVICE=proxy -t diffsurge-proxy -f Dockerfile .
#
# See Dockerfile for the full build definition.
FROM scratch

