# DEPRECATED — replaced by the consolidated Dockerfile.
#
# Build the replayer service with:
#   docker build --build-arg SERVICE=replayer -t diffsurge-replayer -f Dockerfile .
#
# See Dockerfile for the full build definition.
FROM scratch

