# DEPRECATED — replaced by the consolidated Dockerfile.
#
# Build the API service with:
#   docker build --build-arg SERVICE=api -t diffsurge-api -f Dockerfile .
#
# See Dockerfile for the full build definition.
FROM scratch

